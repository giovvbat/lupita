procedure main() {
    n int = 10;
    n = scan();

    if (n < 0) {
        print(n + " is negative");
    } else {
        if (n > 0) {
            print(n + " is positive");
        } else {
            print(n + " is zero");
        }
    }
}
