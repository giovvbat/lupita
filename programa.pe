procedure main() {
    a vector<vector<int>>;
    
    for (i int = 0; i < 3; i += 1) {
        line vector<int>;

        for (j int = 0; j < 2; j += 1) {
            line = line + (i + j);
        }

        a = a + line;
    }

    print(a);
}