function soma(c int, b int) int {
    return c + b;
}

procedure example(r int) {
    r = 7;
    //return i;
}

procedure main() {
    a const int = 4;
    //print(a);
    //example(a);
    kjdn guess = soma(a, a);
}

