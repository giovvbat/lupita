struct Pessoa {
    nome string = "Amanda";
}

function say_hello(name string) string {
    return ("Hello, " + name + "!");
}

procedure main() {
    p Pessoa;
    print(say_hello(p.nome));
}