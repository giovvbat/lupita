struct Pessoa {
    nome string = "exemplo de inconstante";
    idade int = 21;
    idadee int;
}

struct Pessoa2 {
    nome string;
}

enum Pessoa3 {
    p Pessoa;
    s float;
    w float;
    q int;
}

procedure main() {
    q Pessoa;
    p const Pessoa = q;
    s guess = q;

    print(p);
}

// access_chain precisa retornar tipo e nome